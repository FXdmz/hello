{
  "name": "express-postgres-server",
  "version": "1.0.0",
  "description": "Express server with PostgreSQL for note taking",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "sops exec-env secrets.sops.env 'node --test test/*.test.js'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
